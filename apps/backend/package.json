{
  "name": "@orchestra/backend",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "convex dev",
    "build": "echo 'No build step for Convex backend'",
    "test": "bun test convex",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "convex": "^1"
  },
  "devDependencies": {
    "@types/node": "^25.5.0"
  }
}
