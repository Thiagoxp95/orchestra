# Production Convex deployment (linked project "orchestra" → prod).
# Public, non-secret URL — safe to commit. Vercel dashboard env vars, if set,
# override this. Keep in sync with apps/desktop/.env.
NEXT_PUBLIC_CONVEX_URL=https://fearless-pika-904.convex.cloud
